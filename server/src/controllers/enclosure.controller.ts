import { NextFunction, Request, Response } from "express";


const createEnclosure=async (req:Request,res:Response,next:NextFunction) => {
    try {
        
    } catch (error) {
        next(error)
    }
}

const updateEnclosure=async (req:Request,res:Response,next:NextFunction) => {
    try
}